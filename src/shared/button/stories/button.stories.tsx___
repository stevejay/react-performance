import React from "react";
import { Button } from "src/shared/button";

const Default = () => (
  <Button variant="primary" useCSSAnimation onClick={() => {}}>
    Some content
  </Button>
);

// eslint-disable-next-line import/no-default-export
export default {
  component: Button,
  title: "Button"
};

export { Default };
